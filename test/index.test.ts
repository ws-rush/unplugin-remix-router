// test/index.test.ts
import { describe, expect, it } from 'vitest'

// Adjust the path based on your actual folder structure

describe('not yet', () => {
  it('not yet also', async () => {
    expect(1).toBe(1)
  })
})
